services:
  svbridge:
    build: .
    ports:
      - "${PORT:-8086}:8086"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./${SA_FILE:-sa.json}:/app/sa.json:ro
    restart: unless-stopped
